extends ./layout/index

block contenido
    main.container.mt-5 
        .row
            h1(class="mt-5 text-center") #{pagina}
            main.container.mt-5
                include ./layout/bloque_viajes